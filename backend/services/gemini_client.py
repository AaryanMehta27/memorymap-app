# All Gemini API interactions go here.
# Never import google.generativeai outside of this file.

# TODO: Initialize Gemini 2.0 Flash client using GEMINI_API_KEY
# TODO: analyze_image(image_bytes, mime_type) -> dict — send image with structured prompt, return parsed tags
# TODO: query_with_context(context, question) -> str — text-only query for conversational interface
# TODO: suggest_positions(tags, room_dims) -> list — interpret position descriptions as coordinates
# TODO: Always include privacy system instruction on every vision call
