# Read/write tags to Supabase

# TODO: Initialize Supabase client using SUPABASE_URL and SUPABASE_SERVICE_KEY
# TODO: save_tags(room_id, photo_id, tags) -> list — insert tags into Supabase tags table
# TODO: get_tags_for_home(home_id) -> list — fetch all tags for a home, joined with room name and photo path
# TODO: get_tags_for_room(room_id) -> list — fetch tags for a specific room
