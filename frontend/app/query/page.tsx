// TODO: Query page — conversational interface for patients.
// Text input for questions like "Where are my pills?"
// Sends to /api/query/ask, displays answer with source photo.
export default function QueryPage() {
  return <div>Query Page</div>;
}
