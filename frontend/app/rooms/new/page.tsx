// TODO: New room page — form to create a room (name, photo upload/capture),
// send photo to /api/vision/analyze, display returned tags,
// show floor plan canvas for tag placement.
export default function NewRoomPage() {
  return <div>New Room Page</div>;
}
